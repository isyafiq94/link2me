package com.example.peroduascanner.kwp

/**
 * Parser for KWP DTC (Diagnostic Trouble Codes).
 */
object KwpDtcParser {
    fun parseDtc(data: ByteArray): List<String> {
        // Placeholder: real KWP DTC formats vary. Return an empty list for now.
        return emptyList()
    }
}
